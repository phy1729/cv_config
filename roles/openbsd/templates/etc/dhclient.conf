initial-interval 1;
send host-name "CV-{{ name }}";
require domain-name-servers;
supersede host-name "{{ name }}";
supersede domain-name "collegiumv.org";
initial-interval 1;
send host-name "{{ name }}";
request domain-name-servers;
