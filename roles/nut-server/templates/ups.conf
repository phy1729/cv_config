{% for unit in connected_UPS %}
[{{ connected_UPS[unit].name }}]
driver={{ connected_UPS[unit].driver }}
port={{ connected_UPS[unit].port }}
desc={{ connected_UPS[unit].desc }}
serial={{ connected_UPS[unit].serial }}
{% endfor %}
